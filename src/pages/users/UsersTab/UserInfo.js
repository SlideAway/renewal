import React from 'react';
import PropTypes from 'prop-types';

const UserInfo = props => {
    return (
        <>
            사용자 정보
        </>
    );
};

UserInfo.propTypes = {

};

export default UserInfo;