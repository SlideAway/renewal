import React from 'react';
import {CLabel} from "@coreui/react";

const HsLabel = ({name}) => {

    return (
        <>
            <CLabel>{name}</CLabel>
        </>
    );
};

export default HsLabel;
