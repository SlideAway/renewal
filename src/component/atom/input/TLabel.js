import React from 'react';
import {CLabel} from "@coreui/react";

const TLabel = ({name}) => {

    return (
        <>
            <CLabel>{name}</CLabel>
        </>
    );
};

export default TLabel;
